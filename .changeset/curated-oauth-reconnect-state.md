---
"@roomote/web": patch
---

Show reconnect state after an integration's OAuth grant is rejected, stop retrying the rejected credentials, and preserve newer credentials when an older refresh fails late.
