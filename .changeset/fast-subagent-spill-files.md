---
"@roomote/cloud-agents": patch
---

Fast conversations no longer expose filesystem subagents that cannot access local tools, and oversized integration results now give actionable inline truncation guidance instead of pointing those subagents at OpenCode spill files.
