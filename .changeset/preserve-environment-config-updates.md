---
"@roomote/cloud-agents": patch
"@roomote/worker": patch
---

Preserve unrelated environment settings when an agent revises an existing environment definition, and clarify that update calls replace the full definition.
