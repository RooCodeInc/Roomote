---
"@roomote/web": minor
---

Replace the final setup step with preselected starter tasks (speed up CI, security scan, fix test flakes, update dependencies) that launch as standard tasks when setup completes.
