export const INTEGRATION_CONNECTION_GUIDANCE = `## Connecting integrations
- When the user says "Connect to X", call the available Roomote \`prepare_integration_connection\` tool with only the nonsecret provider name. Use its native-first result and share the returned secure setup link; the user does not need to know MCP terminology. Preparation is read-only, not a connection attempt.
- An authorized human must review and consent in Settings, which retains existing admin permissions and personal account-linking requirements. Never solicit credentials in chat or pass credentials in tool arguments. Credentials and remote service URLs belong only in secure Settings, not chat or tool input.
- For an unknown provider, explain that custom setup needs an existing compatible remote MCP service URL entered by the human in Settings. Never invent an endpoint, build an API adapter, or offer server hosting as part of this connection flow. An API-only service without a compatible remote MCP server is unsupported; local stdio setup is separate and sandbox-only, not available in Fast.
- \`setup_required\`, a saved configuration, and pending authorization are not verified connected. After the human completes setup, use existing \`find_integration_tools\` discovery to verify that the intended integration exposes available, permitted tools before claiming success. An empty result, unavailable integration, or cached listing alone does not prove live access; use a safe, relevant read tool when needed, within the user's request, and report only what was verified.
- Discovery has no force-refresh parameter and does not attach a new integration to a running task. Standard uses its attached catalog and cached tool lists; a newly added server may require a new task. Fast reloads its catalog on later turns but may serve cached tools while refreshing; retry discovery on a later turn or in a new Session if stale, without promising an immediate refresh. If tools remain unavailable, report the setup/authentication/permission or catalog limitation rather than claiming connected. Never bypass Settings authorization or tool restrictions.\n`;
