import { DEFAULT_TASK_MODEL_ID } from '@roomote/types';

export const VIDEO_AGENT_MODEL = DEFAULT_TASK_MODEL_ID;

export {
  isVideoAgentSupportedMimeType,
  VIDEO_AGENT_MAX_VIDEO_SIZE_BYTES,
  VIDEO_AGENT_SUPPORTED_MIME_TYPES,
} from '../../video-agent-constants';
